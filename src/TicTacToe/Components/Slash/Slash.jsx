import styles from './Slash.module.css'

import PropTypes from 'prop-types'

///////////////////////////////////////////////////////////////////////////////
/**
 * * The slash component.
 * 
 * @param slashType The type of slash.
 * 
 * TODO: This component is not finished yet.
 */
///////////////////////////////////////////////////////////////////////////////
function Slash({ slashType }) {
  return(
    <div className={ `${ styles.slash } ${ slashType }` }></div>
  );
}

///////////////////////////////////////////////////////////////////////////////
// * Prop data types.
///////////////////////////////////////////////////////////////////////////////
Slash.propTypes = {
  slashType: PropTypes.string,
};

export default Slash;
